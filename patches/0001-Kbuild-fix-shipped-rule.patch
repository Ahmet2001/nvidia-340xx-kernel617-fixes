--- a/Kbuild
+++ b/Kbuild
@@ -67,3 +67,8 @@
 	nvidia_uvm_page_cache.o \
 	nvidia_uvm_utils.o \
 	uvm_gpu_ops_tests.o
+
+# Modern kbuild removed the automatic "%_shipped -> %" copy rule, so we
+# need to provide it explicitly for the prebuilt binary blob.
+$(obj)/nv-kernel.o: $(src)/nv-kernel.o_shipped
+	@cp -f $< $@
